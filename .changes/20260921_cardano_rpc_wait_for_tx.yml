project: cardano-rpc
pr: 1351
kind:
  - feature
description: |
  Add the `WaitForTx` method to the UTxO RPC submit service, which streams the progress of the given transactions until they are confirmed on chain.
